# Demo

Build the package and run:

```bash
npm run build
node demo/example.mjs
```

The demo prints a minimal demo manifest for Player System scenarios.

The default catalog now covers:

- awakening coaching
- mission guidance
- focused pane composition
- combat-safe tutorial reduction
- institution-routing handoffs
- points-ledger spend previews
- scaled composition portability evidence
